<UserControl x:Class="Ace.App.Controls.RankBadgeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="20">
    <Border x:Name="EpauletteBorder"
            Background="{DynamicResource EpauletteBackgroundBrush}"
            CornerRadius="3"
            Padding="2"
            BorderBrush="{DynamicResource GoldBrush}"
            BorderThickness="1">
        <Grid x:Name="StripesContainer">
        </Grid>
    </Border>
</UserControl>

<UserControl x:Class="Ace.App.Views.Menus.PilotMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <TabControl>
            <TabItem Header="Profile">
                <Grid Background="{StaticResource BgPrimary}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="20">
                        <Border Width="200" Height="200" CornerRadius="100" BorderBrush="{StaticResource BorderBrush}" BorderThickness="2" Background="{StaticResource CardBg}" ClipToBounds="True">
                            <Image Name="PilotImage" Stretch="UniformToFill" />
                        </Border>
                        
                        <Button Content="Change Image" Margin="0,15,0,0" Click="BtnChangeImage_Click" HorizontalAlignment="Center"/>
                        
                        <Border Background="{StaticResource CardBg}" Margin="0,20,0,0" Padding="15" CornerRadius="4" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="TOTAL FLIGHT TIME" FontSize="11" FontWeight="Bold" Foreground="{StaticResource SubtleForegroundBrush}"/>
                                <TextBlock Name="TxtTotalTime" Text="00:00:00" Foreground="{StaticResource AccentBrush}" FontSize="28" FontWeight="Bold" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="20,40,20,0">
                        <TextBlock Text="PILOT DATA" Style="{StaticResource HeadingText}" FontSize="20" Margin="0,0,0,25"/>
                        
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Name" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Name="TxtName" />
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Birthday" VerticalAlignment="Center"/>
                            <DatePicker Grid.Column="1" Name="DpBirthday" />
                        </Grid>

                        <Button Content="Save" HorizontalAlignment="Right" Width="120" Margin="0,30,0,0" Click="BtnSave_Click" 
                                Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Foreground="White"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            
            <TabItem Header="Licenses">
                <Grid Background="{StaticResource BgPrimary}" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Acquired Licenses" Style="{StaticResource HeadingText}" FontSize="20" Margin="10,10,10,20"/>

                    <DataGrid Grid.Row="1" Name="LicensesGrid" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="License" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Issued On" Binding="{Binding IssuedDate, StringFormat=d}" Width="150"/>
                            <DataGridTextColumn Header="Authority" Binding="{Binding IssuingAuthority}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Type Ratings">
                <Grid Background="{StaticResource BgPrimary}" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Earned Type Ratings" Style="{StaticResource HeadingText}" FontSize="20" Margin="10,10,10,20"/>

                    <DataGrid Grid.Row="1" Name="TypeRatingsGrid" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Aircraft Type" Binding="{Binding AircraftType}" Width="*"/>
                            <DataGridTextColumn Header="Earned On" Binding="{Binding EarnedDate, StringFormat=d}" Width="150"/>
                            <DataGridTextColumn Header="Authority" Binding="{Binding IssuingAuthority}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>

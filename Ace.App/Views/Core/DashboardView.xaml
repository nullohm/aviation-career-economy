<UserControl x:Class="Ace.App.Views.Core.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Ace.App.Converters"
             xmlns:core="clr-namespace:Ace.App.Views.Core"
             xmlns:controls="clr-namespace:Ace.App.Controls"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450" d:DesignWidth="600"
             Loaded="DashboardView_Loaded">
    <UserControl.Resources>
        <converters:FlightPlanStatusToColorConverter x:Key="FlightPlanStatusToColorConverter"/>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Dashboard" Style="{StaticResource HeadingText}" FontSize="28" VerticalAlignment="Center"/>

            <!-- Balance (like Finance view) -->
            <Border Grid.Column="1" Background="{StaticResource BgSecondary}" CornerRadius="6" Padding="16,8" Margin="0,0,16,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Balance, StringFormat={}{0:N0} €}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
            </Border>

            <!-- Connection Status -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="10" Height="10" Margin="0,0,8,0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="{StaticResource SubtleForegroundBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Fill" Value="{StaticResource SuccessBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding ConnectionStatus}" Foreground="{StaticResource SubtleForegroundBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Row 1: Gauges -->
        <Border Grid.Row="1" Style="{StaticResource GlassCardStyle}" Margin="0,0,0,16" Padding="12,8">
                <UniformGrid Columns="8" Rows="1">
                        <Border Style="{StaticResource GaugeStyle}" Margin="8">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="ALT" Style="{StaticResource GaugeLabelStyle}"/>
                                <TextBlock Text="{Binding Altitude, StringFormat={}{0:N0}}" Style="{StaticResource GaugeValueStyle}"/>
                                <TextBlock Text="ft" Style="{StaticResource GaugeUnitStyle}"/>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource GaugeStyle}" Margin="8">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="GS" Style="{StaticResource GaugeLabelStyle}"/>
                                <TextBlock Text="{Binding GroundSpeed, StringFormat={}{0:N0}}" Style="{StaticResource GaugeValueStyle}"/>
                                <TextBlock Text="kts" Style="{StaticResource GaugeUnitStyle}"/>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource GaugeStyle}" Margin="8">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="HDG" Style="{StaticResource GaugeLabelStyle}"/>
                                <TextBlock Text="{Binding Heading, StringFormat={}{0:N0}}" Style="{StaticResource GaugeValueStyle}"/>
                                <TextBlock Text="deg" Style="{StaticResource GaugeUnitStyle}"/>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource GaugeStyle}" Margin="8">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="VS" Style="{StaticResource GaugeLabelStyle}"/>
                                <TextBlock Text="{Binding VerticalSpeed, StringFormat={}{0:N0}}" Style="{StaticResource GaugeValueStyle}"/>
                                <TextBlock Text="fpm" Style="{StaticResource GaugeUnitStyle}"/>
                            </StackPanel>
                        </Border>
                        <Border Margin="8">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource GaugeStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AoAWarning}" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding AoACritical}" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="AOA" Style="{StaticResource GaugeLabelStyle}"/>
                                <TextBlock Text="{Binding AoAText}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource GaugeValueStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding AoAWarning}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding AoACritical}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="deg" Style="{StaticResource GaugeUnitStyle}"/>
                            </StackPanel>
                        </Border>
                        <Border Margin="8">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource GaugeStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding FuelLow}" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="FUEL" Style="{StaticResource GaugeLabelStyle}"/>
                                <TextBlock Text="{Binding FuelPercentText}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource GaugeValueStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding FuelLow}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="%" Style="{StaticResource GaugeUnitStyle}"/>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource GaugeStyle}" Margin="8">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="FF" Style="{StaticResource GaugeLabelStyle}"/>
                                <TextBlock Text="{Binding FuelFlowText}" Style="{StaticResource GaugeValueStyle}"/>
                                <TextBlock Text="gph" Style="{StaticResource GaugeUnitStyle}"/>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource GaugeStyle}" Margin="8">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="SIM" Style="{StaticResource GaugeLabelStyle}"/>
                                <TextBlock Text="{Binding SimulationRateText}" Style="{StaticResource GaugeValueStyle}"/>
                                <TextBlock Text="rate" Style="{StaticResource GaugeUnitStyle}"/>
                            </StackPanel>
                        </Border>
                </UniformGrid>
            </Border>

        <!-- Row 2: Map (50%) + Flight Info (50%) -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Mini Map -->
            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}" Padding="0">
                <controls:FlightMiniMapControl x:Name="MiniMapControl"
                                               DepartureIcao="{Binding DepartureIcao}"
                                               ArrivalIcao="{Binding ArrivalIcao}"
                                               AircraftLatitude="{Binding Latitude}"
                                               AircraftLongitude="{Binding Longitude}"
                                               AircraftHeading="{Binding Heading}"
                                               IsActive="{Binding HasActiveFlightPlan}"
                                               MapClicked="MiniMapControl_MapClicked"/>
            </Border>

            <!-- Right: Flight Info Tile -->
            <Border Grid.Column="2" Style="{StaticResource GlassCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header with Route Info -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12" Visibility="{Binding HasActiveFlightPlan, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Border Background="{StaticResource SuccessBrush}" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                            <TextBlock Text="{Binding DepartureIcao}" FontWeight="Bold" FontSize="14" Foreground="White"/>
                        </Border>
                        <TextBlock Text="→" FontSize="20" Foreground="{StaticResource SubtleForegroundBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Border Background="#E91E63" CornerRadius="4" Padding="8,4" Margin="0,0,16,0">
                            <TextBlock Text="{Binding ArrivalIcao}" FontWeight="Bold" FontSize="14" Foreground="White"/>
                        </Border>
                        <Border Background="{StaticResource BgSecondary}" CornerRadius="4" Padding="10,5">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconAircraft}" Fill="{StaticResource AccentBrush}" Stretch="Uniform" Width="12" Height="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding FlightPlanAircraftRegistration}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource ForegroundBrush}" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding FlightPlanAircraftType, StringFormat=' ({0})'}" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="0" Text="FLIGHT INFO" Foreground="{StaticResource SubtleForegroundBrush}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,12" Visibility="{Binding HasActiveFlightPlan, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"/>

                    <!-- Info Items - 2 columns layout -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Column: Aircraft Info -->
                        <StackPanel Grid.Column="0">
                            <!-- SimConnect Aircraft -->
                            <Border Background="{StaticResource BgSecondary}" Margin="0,0,0,8" Padding="10,8" CornerRadius="4">
                                <StackPanel>
                                    <TextBlock Text="AIRCRAFT" FontSize="10" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Aircraft}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                </StackPanel>
                            </Border>

                            <!-- Flight Status -->
                            <Border Background="{StaticResource BgSecondary}" Margin="0,0,0,8" Padding="10,8" CornerRadius="4">
                                <StackPanel>
                                    <TextBlock Text="STATUS" FontSize="10" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding FlightStatusText}" FontSize="14" FontWeight="Bold" Foreground="{Binding FlightStatusText, Converter={StaticResource FlightPlanStatusToColorConverter}}"/>
                                </StackPanel>
                            </Border>

                            <!-- Flight Plan Details Row -->
                            <Border Background="{StaticResource BgSecondary}" Margin="0,0,0,8" Padding="10,8" CornerRadius="4" Visibility="{Binding HasActiveFlightPlan, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,4">
                                        <TextBlock Text="PAX" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding FlightPlanPassengersText}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,4">
                                        <TextBlock Text="CARGO" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding FlightPlanCargoText}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="0">
                                        <TextBlock Text="COURSE" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding CourseToDestText}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="1">
                                        <TextBlock Text="DISTANCE" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding TotalDistanceNM, StringFormat={}{0:F0} NM}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Cruise/Ceiling Row -->
                            <Grid Visibility="{Binding HasActiveFlightPlan, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{StaticResource BgSecondary}" Padding="10,8" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="CRUISE" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding AircraftCruiseSpeedText}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Background="{StaticResource BgSecondary}" Padding="10,8" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="CEILING" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding AircraftServiceCeilingText}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>

                        <!-- Right Column: Progress/Descent/Fuel Info -->
                        <StackPanel Grid.Column="2" Visibility="{Binding HasActiveFlightPlan, Converter={StaticResource BoolToVisibilityConverter}}">
                            <!-- Progress Stats Row -->
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{StaticResource BgSecondary}" Padding="10,8" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="PROGRESS" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding FlightProgressPercent}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Background="{StaticResource BgSecondary}" Padding="10,8" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="REMAINING" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding DistanceRemainingText}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="4" Background="{StaticResource BgSecondary}" Padding="10,8" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="ETA" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding EstimatedTimeRemaining}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Top of Descent -->
                            <Border Margin="0,0,0,8" Padding="10,8" CornerRadius="4">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource BgSecondary}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ShouldStartDescent}" Value="True">
                                                <Setter Property="Background" Value="#3D2C10"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="TOP OF DESCENT" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock FontSize="14" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                                                    <Setter Property="Text" Value="{Binding TopOfDescentDistanceText}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ShouldStartDescent}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                                                            <Setter Property="Text" Value="NOW"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="↓" FontSize="18" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{StaticResource SubtleForegroundBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ShouldStartDescent}" Value="True">
                                                        <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>

                            <!-- Descent Rate / Fuel Row -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- Recommended Descent Rate -->
                                <Border Grid.Column="0" Background="{StaticResource BgSecondary}" Padding="10,8" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="DESCENT (3°)" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding RecommendedDescentRateText}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Fuel at Destination -->
                                <Border Grid.Column="2" Padding="10,8" CornerRadius="4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{StaticResource BgSecondary}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding FuelAtDestinationLow}" Value="True">
                                                    <Setter Property="Background" Value="#3D1A1A"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel>
                                        <TextBlock Text="FUEL AT DEST" FontSize="9" Foreground="{StaticResource SubtleForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock FontSize="13" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                                                    <Setter Property="Text" Value="{Binding FuelAtDestinationText}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding FuelAtDestinationLow}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Row 3: Flight Progress Section (full width) -->
        <Border Grid.Row="3" x:Name="FlightProgressPanel" Style="{StaticResource GlassCardStyle}" Margin="0,0,0,16" Visibility="{Binding HasActiveFlightPlan, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: Altitude Profile Chart -->
                <Grid Grid.Row="0" Height="90">
                    <oxy:PlotView x:Name="AltitudeChartView" Model="{Binding AltitudeProfileModel}" Background="Transparent" Margin="0" MouseLeftButtonDown="AltitudeChartView_MouseLeftButtonDown"/>
                </Grid>

                <!-- Row 1: Progress Bar -->
                <Grid Grid.Row="1" Margin="0,8,0,0" x:Name="ProgressBarContainer">
                    <Border Background="#1A1A2A" CornerRadius="6" Height="8"/>
                    <Grid HorizontalAlignment="Left">
                        <Grid.Width>
                            <MultiBinding Converter="{StaticResource PercentToWidthMultiConverter}">
                                <Binding Path="FlightProgress"/>
                                <Binding Path="ActualWidth" ElementName="ProgressBarContainer"/>
                            </MultiBinding>
                        </Grid.Width>
                        <Border Background="{StaticResource AccentBrush}" CornerRadius="6" Height="8"/>
                    </Grid>
                </Grid>
            </Grid>
        </Border>

        <!-- Row 4: Notifications Panel -->
        <core:NotificationsPanel Grid.Row="4" x:Name="NotificationsPanelControl"/>
    </Grid>
</UserControl>

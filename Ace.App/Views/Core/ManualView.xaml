<UserControl x:Class="Ace.App.Views.Core.ManualView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mdxaml="clr-namespace:MdXaml;assembly=MdXaml"
             mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ManualMarkdownStyle" TargetType="mdxaml:MarkdownScrollViewer">
            <Setter Property="Background" Value="{StaticResource BgPrimary}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MarkdownStyleName" Value="SasabuneStandard"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BgPrimary}">
        <mdxaml:MarkdownScrollViewer x:Name="MarkdownViewer"
                                      Style="{StaticResource ManualMarkdownStyle}"
                                      Margin="24"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      ClickAction="DisplayWithRelativePath"/>
    </Grid>
</UserControl>

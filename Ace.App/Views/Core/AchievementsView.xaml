<UserControl x:Class="Ace.App.Views.Core.AchievementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             Loaded="OnLoaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Achievements" Style="{StaticResource HeadingText}" FontSize="24"/>
                <TextBlock x:Name="TxtProgress" Text="0 / 0 unlocked" Foreground="{StaticResource SubtleForegroundBrush}" FontSize="14" Margin="0,4,0,0"/>
            </StackPanel>

            <Border Grid.Column="1" Background="{StaticResource CardBg}" CornerRadius="8" Padding="20,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Completion: " Foreground="{StaticResource SubtleForegroundBrush}" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtCompletionPercent" Text="0%" FontSize="24" FontWeight="Bold" Foreground="#4CAF50" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Category Filter -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,16">
            <RadioButton x:Name="RbAll" Content="All" IsChecked="True" GroupName="Category" Checked="CategoryFilter_Changed" Style="{StaticResource CategoryRadioButtonStyle}"/>
            <RadioButton x:Name="RbFlights" Content="Flights" GroupName="Category" Checked="CategoryFilter_Changed" Style="{StaticResource CategoryRadioButtonStyle}"/>
            <RadioButton x:Name="RbDistance" Content="Distance" GroupName="Category" Checked="CategoryFilter_Changed" Style="{StaticResource CategoryRadioButtonStyle}"/>
            <RadioButton x:Name="RbFleet" Content="Fleet" GroupName="Category" Checked="CategoryFilter_Changed" Style="{StaticResource CategoryRadioButtonStyle}"/>
            <RadioButton x:Name="RbFinance" Content="Finance" GroupName="Category" Checked="CategoryFilter_Changed" Style="{StaticResource CategoryRadioButtonStyle}"/>
            <RadioButton x:Name="RbFBOs" Content="FBOs" GroupName="Category" Checked="CategoryFilter_Changed" Style="{StaticResource CategoryRadioButtonStyle}"/>
            <RadioButton x:Name="RbPilots" Content="Pilots" GroupName="Category" Checked="CategoryFilter_Changed" Style="{StaticResource CategoryRadioButtonStyle}"/>
            <RadioButton x:Name="RbSpecial" Content="Special" GroupName="Category" Checked="CategoryFilter_Changed" Style="{StaticResource CategoryRadioButtonStyle}"/>
        </WrapPanel>

        <!-- Achievements Grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="AchievementsPanel">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="280" Margin="0,0,16,16" CornerRadius="8" Padding="16">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{StaticResource CardBg}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="#1a4CAF50" Offset="0"/>
                                                        <GradientStop Color="#0d2e7d32" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="BorderBrush" Value="#4CAF50"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon -->
                                <Border Width="56" Height="56" CornerRadius="28" VerticalAlignment="Top">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#2a2a2a"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                <GradientStop Color="#FFD700" Offset="0"/>
                                                                <GradientStop Color="#FFA500" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Icon}" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Opacity" Value="0.4"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                                        <Setter Property="Opacity" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>

                                <!-- Content -->
                                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                    <!-- Title & Tier -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource SubtleForegroundBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <Border Grid.Column="1" CornerRadius="4" Padding="6,2">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#3a3a3a"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Tier}" Value="Bronze">
                                                            <Setter Property="Background" Value="#8B4513"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Tier}" Value="Silver">
                                                            <Setter Property="Background" Value="#6a6a6a"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Tier}" Value="Gold">
                                                            <Setter Property="Background" Value="#B8860B"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Tier}" Value="Platinum">
                                                            <Setter Property="Background" Value="#4a7c7c"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Tier}" FontSize="10" Foreground="White"/>
                                        </Border>
                                    </Grid>

                                    <!-- Description -->
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{StaticResource SubtleForegroundBrush}" TextWrapping="Wrap" Margin="0,4,0,8"/>

                                    <!-- Progress Bar -->
                                    <Grid Height="6" Margin="0,0,0,4">
                                        <Border Background="#2a2a2a" CornerRadius="3"/>
                                        <Border CornerRadius="3" HorizontalAlignment="Left" Width="{Binding ProgressPercent, Converter={StaticResource PercentToWidthConverter}, ConverterParameter=180}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#4CAF50"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsUnlocked}" Value="False">
                                                            <Setter Property="Background" Value="#666666"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </Grid>

                                    <!-- Progress Text & Reward -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding ProgressText}" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}"/>
                                        <TextBlock Grid.Column="1" FontSize="11" Foreground="#FFD700">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="{Binding Reward, StringFormat={}+€{0:N0}}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Reward}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>

                                    <!-- Unlocked Date -->
                                    <TextBlock FontSize="10" Foreground="#4CAF50" Margin="0,4,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Setter Property="Text" Value="{Binding UnlockedDate, StringFormat={}Unlocked: {0:dd.MM.yyyy}}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>

<Window x:Class="Ace.App.Views.Dialogs.SelectAircraftImageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Select Aircraft Image"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{StaticResource ForegroundBrush}"
        Width="466" Height="416"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32" CornerRadius="8" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{StaticResource BgPrimary}"
            BorderBrush="#50FFFFFF"
            BorderThickness="1"
            CornerRadius="8"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.6" ShadowDepth="4"/>
        </Border.Effect>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0" Background="{StaticResource CardBg}" CornerRadius="8,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TitleText" Text="Select Aircraft Image" FontSize="13" Style="{StaticResource HeadingText}"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>

                <Button Grid.Column="1" Name="BtnClose" Width="36" Height="24" Content="{StaticResource IconClose}"
                        Click="BtnClose_Click" ToolTip="Close"
                        Style="{StaticResource WindowCloseButtonStyle}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" Margin="0,0,8,0"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Aircraft Name -->
            <TextBlock x:Name="TxtAircraftName" Grid.Row="0" FontSize="16" FontWeight="SemiBold"
                       Margin="0,0,0,16"/>

            <!-- Image Preview -->
            <Border Grid.Row="1" Background="{StaticResource CardBg}" CornerRadius="8"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <Grid>
                    <Image x:Name="ImagePreview" Stretch="Uniform" Margin="16"/>
                    <TextBlock x:Name="TxtNoImage" Text="No image selected"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{StaticResource SubtleForegroundBrush}"
                               Visibility="Collapsed"/>
                </Grid>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                <Button x:Name="BtnBrowse" Content="Browse..." Width="100" Height="32"
                        Click="BtnBrowse_Click" Margin="0,0,8,0"
                        Style="{StaticResource SecondaryButtonStyle}"/>
                <Button x:Name="BtnReset" Content="Reset to Default" Width="120" Height="32"
                        Click="BtnReset_Click"
                        Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>
        </Grid>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <Button x:Name="BtnCancel" Content="Cancel" Width="80" Height="32"
                    Click="BtnCancel_Click" Margin="0,0,8,0"
                    Style="{StaticResource SecondaryButtonStyle}"/>
            <Button x:Name="BtnSave" Content="Save" Width="80" Height="32"
                    Click="BtnSave_Click"
                    Style="{StaticResource SuccessButtonStyle}"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>

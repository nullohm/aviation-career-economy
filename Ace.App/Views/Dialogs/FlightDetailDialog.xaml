<Window x:Class="Ace.App.Views.Dialogs.FlightDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Flight Details"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{StaticResource ForegroundBrush}"
        Width="496" Height="536"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" CornerRadius="8" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{StaticResource BgPrimary}"
            BorderBrush="#50FFFFFF"
            BorderThickness="1"
            CornerRadius="8"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.6" ShadowDepth="4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource CardBg}" CornerRadius="8,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="TitleText" Text="Flight Details" FontSize="14" Style="{StaticResource HeadingText}"
                               VerticalAlignment="Center" Margin="16,0,0,0"/>

                    <Button Grid.Column="1" Name="BtnClose" Width="32" Height="28" Content="{StaticResource IconClose}"
                            Click="BtnClose_Click" ToolTip="Close"
                            Style="{StaticResource WindowCloseButtonStyle}"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True" Margin="0,0,4,0"/>
                </Grid>
            </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- Flight Route Header -->
                <Border Background="{StaticResource CardBg}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock x:Name="TxtDeparture" Text="EDDS" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Departure" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="->" FontSize="24" VerticalAlignment="Center" Margin="20,0"
                                   Foreground="{StaticResource SubtleForegroundBrush}"/>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <TextBlock x:Name="TxtArrival" Text="EDDM" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Arrival" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Flight Info -->
                <Border Background="{StaticResource CardBg}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,12">
                            <TextBlock Text="Aircraft" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}"/>
                            <TextBlock x:Name="TxtAircraft" Text="D-INIT" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                            <TextBlock Text="Date" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}"/>
                            <TextBlock x:Name="TxtDate" Text="31.12.2025" FontSize="14"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,12">
                            <TextBlock Text="Distance" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}"/>
                            <TextBlock x:Name="TxtDistance" Text="100 NM" FontSize="14"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,12">
                            <TextBlock Text="Duration" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}"/>
                            <TextBlock x:Name="TxtDuration" Text="0:50" FontSize="14"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,12">
                            <TextBlock Text="Passengers" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}"/>
                            <TextBlock x:Name="TxtPassengers" Text="3 PAX" FontSize="14"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,12">
                            <TextBlock Text="Landing Rate" FontSize="11" Foreground="{StaticResource SubtleForegroundBrush}"/>
                            <TextBlock x:Name="TxtLandingRate" Text="-248 fpm" FontSize="14"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Cost Breakdown -->
                <Border Background="{StaticResource CardBg}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Cost Breakdown" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,12"/>

                        <Grid x:Name="CostGrid">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Fuel" Foreground="{StaticResource SubtleForegroundBrush}" Margin="0,4"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtFuel" Text="-43.00" Foreground="#F44336" TextAlignment="Right" Margin="0,4"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Maintenance" Foreground="{StaticResource SubtleForegroundBrush}" Margin="0,4"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtMaintenance" Text="-33.72" Foreground="#F44336" TextAlignment="Right" Margin="0,4"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Depreciation" Foreground="{StaticResource SubtleForegroundBrush}" Margin="0,4"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtDepreciation" Text="-9.44" Foreground="#F44336" TextAlignment="Right" Margin="0,4"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Insurance" Foreground="{StaticResource SubtleForegroundBrush}" Margin="0,4"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="TxtInsurance" Text="-1.42" Foreground="#F44336" TextAlignment="Right" Margin="0,4"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Crew" Foreground="{StaticResource SubtleForegroundBrush}" Margin="0,4"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="TxtCrew" Text="-33.21" Foreground="#F44336" TextAlignment="Right" Margin="0,4"/>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Landing Fees" Foreground="{StaticResource SubtleForegroundBrush}" Margin="0,4"/>
                            <TextBlock Grid.Row="5" Grid.Column="1" x:Name="TxtLanding" Text="-50.00" Foreground="#F44336" TextAlignment="Right" Margin="0,4"/>

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Ground Handling" Foreground="{StaticResource SubtleForegroundBrush}" Margin="0,4"/>
                            <TextBlock Grid.Row="6" Grid.Column="1" x:Name="TxtGround" Text="-30.00" Foreground="#F44336" TextAlignment="Right" Margin="0,4"/>

                            <TextBlock Grid.Row="7" Grid.Column="0" Text="Catering" Foreground="{StaticResource SubtleForegroundBrush}" Margin="0,4"/>
                            <TextBlock Grid.Row="7" Grid.Column="1" x:Name="TxtCatering" Text="-24.00" Foreground="#F44336" TextAlignment="Right" Margin="0,4"/>
                        </Grid>

                        <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,12"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Total Costs" FontWeight="SemiBold" Margin="0,4"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtTotalCosts" Text="-834.53" Foreground="#F44336" FontWeight="SemiBold" TextAlignment="Right" Margin="0,4"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Revenue" FontWeight="SemiBold" Margin="0,4"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtRevenue" Text="+894.26" Foreground="#4CAF50" FontWeight="SemiBold" TextAlignment="Right" Margin="0,4"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" x:Name="TxtBonusLabel" Text="Player Bonus" FontWeight="SemiBold" Foreground="#FFC107" Margin="0,4" Visibility="Collapsed"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtBonus" Text="+0.00" Foreground="#FFC107" FontWeight="SemiBold" TextAlignment="Right" Margin="0,4" Visibility="Collapsed"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Profit" FontSize="16" FontWeight="Bold" Margin="0,8,0,4"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="TxtProfit" Text="+59.73" FontSize="16" FontWeight="Bold" Foreground="#4CAF50" TextAlignment="Right" Margin="0,8,0,4"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
                <Button x:Name="BtnOk" Content="Close" Width="100" Height="32" Click="BtnClose_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

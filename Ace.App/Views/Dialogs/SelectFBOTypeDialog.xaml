<Window x:Class="Ace.App.Views.Dialogs.SelectFBOTypeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Rent FBO"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{StaticResource ForegroundBrush}"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32" CornerRadius="8" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{StaticResource BgPrimary}"
            BorderBrush="#50FFFFFF"
            BorderThickness="1"
            CornerRadius="8"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.6" ShadowDepth="4"/>
        </Border.Effect>
    <Grid MinWidth="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBg}" CornerRadius="8,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TitleText" Text="Rent FBO" FontSize="13" Style="{StaticResource HeadingText}"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>

                <Button Grid.Column="1" Name="BtnClose" Width="36" Height="24" Content="{StaticResource IconClose}"
                        Click="BtnClose_Click" ToolTip="Close"
                        Style="{StaticResource WindowCloseButtonStyle}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" Margin="0,0,8,0"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Margin="20,16,20,8">
            <TextBlock x:Name="TxtAirportInfo" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock Text="Select FBO Type:" FontSize="13" Foreground="{StaticResource SubtleForegroundBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="20,8,20,16">
            <RadioButton x:Name="RbLocal" GroupName="FBOType" IsChecked="True" Margin="0,6">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#4CAF50" CornerRadius="3" Padding="8,3" VerticalAlignment="Center">
                        <TextBlock Text="Local" FontSize="11" Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                    <TextBlock x:Name="TxtLocalInfo" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
            </RadioButton>

            <RadioButton x:Name="RbRegional" GroupName="FBOType" Margin="0,6">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#2196F3" CornerRadius="3" Padding="8,3" VerticalAlignment="Center">
                        <TextBlock Text="Regional" FontSize="11" Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                    <TextBlock x:Name="TxtRegionalInfo" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
            </RadioButton>

            <RadioButton x:Name="RbInternational" GroupName="FBOType" Margin="0,6">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#E91E63" CornerRadius="3" Padding="8,3" VerticalAlignment="Center">
                        <TextBlock Text="International" FontSize="11" Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                    <TextBlock x:Name="TxtInternationalInfo" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
            </RadioButton>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <Button x:Name="BtnRent" Content="Rent FBO" Width="100" Height="32" Click="BtnRent_Click"
                    Style="{StaticResource SuccessButtonStyle}" Margin="0,0,10,0"/>
            <Button x:Name="BtnCancel" Content="Cancel" Width="80" Height="32" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>
